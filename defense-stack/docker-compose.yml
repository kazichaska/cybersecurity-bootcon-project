services:
  dozzle:
    image: amir20/dozzle:latest
    container_name: bootcon-dozzle
    ports:
      - "${DOZZLE_PORT:-9999}:8080"
    volumes:
      - "${DOCKER_SOCKET_PATH:-/var/run/docker.sock}:/var/run/docker.sock:ro"
    environment:
      DOZZLE_BASE: "/"
    restart: unless-stopped
